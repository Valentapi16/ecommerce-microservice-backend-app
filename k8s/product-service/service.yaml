apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: ecommerce-staging
  labels:
    app: product-service
    tier: business
spec:
  type: ClusterIP
  ports:
  - port: 8200
    targetPort: 8200
    protocol: TCP
    name: http
  selector:
    app: product-service
