apiVersion: v1
kind: Service
metadata:
  name: cloud-config
  namespace: ecommerce-staging
  labels:
    app: cloud-config
    tier: infrastructure
spec:
  type: ClusterIP
  ports:
  - port: 8888
    targetPort: 8888
    protocol: TCP
    name: http
  selector:
    app: cloud-config
