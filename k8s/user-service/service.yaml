apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: ecommerce-staging
  labels:
    app: user-service
    tier: business
spec:
  type: ClusterIP
  ports:
  - port: 8100
    targetPort: 8100
    protocol: TCP
    name: http
  selector:
    app: user-service
